import React, {Component} from "react";

export class LoginButtonComponent extends Component {
    render() {
        return <a href="#" className="cta">
            <button>Login</button>
        </a>;
    }
}
